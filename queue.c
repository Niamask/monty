#include "monty.h"

/**
 * _queue - function for queue.
 * @pile: the pile
 * @line_number: the number
 *
 * Return: void
 */

void  _queue(stack_t **pile, unsigned  int line_number)
{
	UNUSED(pile);
	UNUSED(line_number);
}
